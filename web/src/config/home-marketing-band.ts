/**
 * Single continuous cream surface for the home marketing stack — no hairline
 * borders between sections (they read as “seams”). Use spacing (py) only.
 */
export const homeMarketingBand = {
  bg: "bg-[#f9f4e8] dark:bg-[#f9f4e8]",
  bodyText: "text-[#2a2218]",
  muted: "text-[#5c4a38]",
} as const;
